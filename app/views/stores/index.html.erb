<% content_for :sidebar do %>
  <%= render 'layouts/main_sidebar' %>
<% end %>

<%= will_paginate %>
<% for store in @stores %>
  <div class="store">
    <strong>Name:</strong> <%= link_to store.name, store_path(store.zid)%>
    </br>
    <strong>City:</strong> <%= store.city.name %>
    </br>
    <strong>Address:</strong> <%= store.address %>
    <div class="">
      <strong>Categories:</strong>
      <% for rel in store.store_type_relationships %>
        &nbsp;<%= rel.store_type.name %>
      <% end %>
    </div>
    <div class="action">
      <%= link_to I18n.t(@button), new_visit_path(:id => store.zid)%>
      <%= link_to I18n.t(@button2), show_menu_store_path(store.zid)%>
    </div>    
  </div>
<% end %>
<%= will_paginate %>

