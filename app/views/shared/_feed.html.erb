<% if @feed_items.any? %>
  <table class="visits" summary="Status feed">
    <%= render :partial => 'shared/feed_item', :collection => @feed_items %>
  </table>
  <%= paginate @feed_items %>
<% end %>